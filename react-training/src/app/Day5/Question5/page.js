// 5. Create a Next.js component that fetches data from a public API (like JSONPlaceholder) using Axios and displays the results on the page. 
// Use the App Router conventions and perform the data fetching on the server side.

import AxiosFetch from "@/Components/Day5Components/AxiosFetch";

function Question5(){
    return(
        <>
        <AxiosFetch/>
        
        </>
    )
}

export default Question5;