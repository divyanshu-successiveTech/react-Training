// 1.Create a React component with an input field. Implement it as a controlled component where the 
// input value is controlled by the component's state. When the user types into the input 
// field, the component's state should update accordingly.

import ControlledComponent from "@/Components/Day4Components/ControlledComponent";

function Question1() {
  return (
    <>
      <ControlledComponent></ControlledComponent>
    </>
  );
}

export default Question1;
