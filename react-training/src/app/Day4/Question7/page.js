// 7.Design a search filter component that consists of an input field. As the user types into the input, 
// use controlled components to filter a list of items displayed below. The list should dynamically update to show only items
//  matching the search query.

import SearchFilter from "@/Components/Day4Components/SearchFilter";

function Question7(){

    return(
        <>
        <SearchFilter/>
        </>
    )

}

export default Question7;